# 교단별 신앙고백·교리 문서 RAG 자동 등록 구현 작업 지시서

> 대상: 현재 개발 중인 설교 생성/RAG 프로그램  
> 작성 기준일: 2026-09-02  
> 기본 구조: PostgreSQL + pgvector + MinIO + 기존 Provider/RAG 계층  
> 목표: 공식 교단 문서의 출처·판본·공식성을 보존하면서 안전하게 수집, 검토, 색인하고 교단별로 격리 검색한다.

---

## 0. Codex 실행 지시

당신은 기존 프로그램을 보존하면서 `교단별 신앙고백·교리 문서 RAG` 기능을 단계적으로 구현하는 수석 백엔드/RAG 엔지니어다.

작업을 시작하기 전에 저장소 전체 구조, `AGENTS.md`, README, 환경설정, 데이터베이스 마이그레이션 체계, 기존 문서 업로드·RAG·임베딩·검색 코드, MinIO 연동, 인증·관리자 권한, 테스트 방식을 먼저 조사하라. 실제 구조를 확인하기 전에는 프레임워크, 디렉터리, 테이블명 또는 실행 명령을 임의로 확정하지 말라.

### 절대 원칙

1. 기존 일반 자료 RAG와 교단 문서 RAG를 논리적으로 분리한다.
2. 승인되지 않은 문서는 설교 생성 검색에 사용하지 않는다.
3. 공식 출처 허용목록에 없는 사이트를 크롤링하지 않는다.
4. 저작권·이용조건이 불명확한 문서의 원문 전체를 자동 수집하거나 재배포하지 않는다.
5. 최신판이 발견되어도 기존 판본을 덮어쓰거나 삭제하지 않는다.
6. 교단과 문서의 공식성은 LLM이 추측하지 못하게 한다.
7. 선택 교단과 다른 교단의 근거가 기본 검색에 섞이지 않게 한다.
8. 데이터베이스 마이그레이션은 하향 호환과 롤백이 가능해야 한다.
9. 기존 사용자 자료와 관계없는 파일은 수정하지 않는다.
10. 비밀키, 관리자 토큰, 내부 주소를 코드·로그·화면에 노출하지 않는다.

---

## 1. 작업 목표와 비목표

### 목표

- 교단 및 공식 자료원 등록·관리
- HTML/PDF/관리자 업로드 문서 수집
- ETag, Last-Modified, SHA-256 기반 변경 감지
- MinIO 원본 및 판본별 스냅샷 보존
- HTML/PDF/OCR 본문 추출
- 편·장·절·조항·문답 중심 구조 기반 청킹
- 관리자 검토·승인 워크플로
- 승인된 청크만 pgvector에 색인
- 교단·판본·언어·공식성 메타데이터 필터 검색
- 일반 검색, 선택 교단, 공통 교리, 교단 비교, 역사 판본 비교 모드
- 답변에 정확한 문서명·교단·판본·조항·공식 URL 표시
- 변경 이력, 실패 로그, 재처리, 롤백 지원

### 이번 작업의 비목표

- 유료 주석·출판물 무단 수집
- 검색엔진을 이용한 무제한 웹 크롤링
- 교리의 옳고 그름을 AI가 판정하는 기능
- 교단 간 차이를 하나의 통합 교리로 자동 합성
- 관리자 승인 없는 완전 자동 공개
- 기존 임베딩 Provider의 전면 교체

---

## 2. 구현 전 조사 및 기준선 보고

코드를 변경하기 전에 다음을 조사하고 짧은 기준선 보고서를 작성하라.

1. 애플리케이션 언어·프레임워크·실행 진입점
2. DB 연결, ORM, 마이그레이션 도구
3. pgvector 확장과 임베딩 차원·거리 연산자
4. MinIO 버킷·객체 키 정책
5. 기존 문서 수집, 파싱, 청킹, 임베딩, 검색 흐름
6. 동기/비동기 작업 큐와 스케줄러 유무
7. 관리자 인증과 권한 모델
8. 기존 중복 방지, 해시, 버전 관리 방식
9. 테스트·린트·타입 검사·브라우저 검증 명령
10. 현재 작업 트리 변경사항과 충돌 가능성

조사 결과를 바탕으로 기존 구성요소를 재사용할 부분과 새로 추가할 부분을 구분하라. 구조 충돌이나 필수 선택지가 발견되면 구현을 확대하지 말고 먼저 보고하라.

---

## 3. 권장 도메인 모델

실제 명명 규칙에 맞게 조정하되 아래 의미를 보존한다.

### 3.1 `denominations`

| 필드 | 요구사항 |
|---|---|
| id | 내부 식별자 |
| code | 고유 코드: KMC, PROK, PCA 등 |
| name_ko / name_en | 교단명 |
| tradition | 장로교·개혁, 감리교·웨슬리안, 침례교, 오순절 등 |
| country | 국가 코드 |
| official_site | 공식 홈페이지 |
| active | 사용 여부 |
| created_at / updated_at | 감사 시각 |

### 3.2 `doctrine_sources`

필수 필드: `denomination_id`, `title`, `document_type`, `source_url`, `source_authority`, `collection_method`, `update_schedule`, `license_status`, `requires_review`, `active`, `last_checked_at`.

`source_authority`는 최소한 다음 값을 지원한다.

- `OFFICIAL_GENERAL_ASSEMBLY`
- `OFFICIAL_DENOMINATION`
- `OFFICIAL_AGENCY`
- `ACADEMIC_REFERENCE`
- `OTHER_REFERENCE`

### 3.3 `doctrine_documents`

필수 필드: `source_id`, `title`, `adoption_year`, `edition`, `language`, `official_status`, `published_at`, `retrieved_at`, `content_hash`, `object_storage_key`, `mime_type`, `review_status`, `supersedes_document_id`, `metadata_json`.

`official_status`는 최소한 다음을 구분한다.

- `OFFICIALLY_ADOPTED`
- `HISTORICAL_STANDARD`
- `STUDY_EDITION`
- `PROPOSED`
- `REFERENCE_ONLY`
- `UNKNOWN`

### 3.4 `doctrine_chunks`

필수 필드: `document_id`, `section_path`, `article_number`, `article_title`, `chunk_index`, `content`, `embedding`, `token_count`, `scripture_refs`, `topic_tags`, `content_hash`.

`document_id + chunk_index`와 문서 내 `content_hash` 중복을 방지한다. 메타데이터 필터용 열에는 적절한 B-tree 인덱스를 추가한다.

### 3.5 `source_snapshots`와 `ingestion_jobs`

원본 확인 시각, HTTP 상태, ETag, Last-Modified, 본문 해시, 객체 키, 변경 감지 여부를 보존한다. 작업 상태·시도 횟수·오류 코드·오류 요약·시작/종료 시각·재처리 관계를 기록한다.

### 3.6 상태 머신

`DISCOVERED → DOWNLOADED → PARSED → NEEDS_REVIEW → APPROVED → INDEXED`

예외 상태는 `REJECTED`, `FAILED`, `SUPERSEDED`, `DISABLED`로 한다. 허용되지 않은 상태 전이는 서버에서 거부한다. `APPROVED` 이전 문서는 실사용 검색 대상이 될 수 없다.

---

## 4. 공식 자료원 허용목록

초기에는 아래 공식 출처를 샘플 또는 시드 후보로 사용하되, 이용조건을 확인한 후 활성화한다.

- 기독교대한감리회: 감리회 신앙고백 및 교리와 장정
- 한국기독교장로회: 신조·신앙고백서
- PCA: Westminster Confession and Catechisms
- SBC: Baptist Faith and Message 2000
- Assemblies of God USA: Statement of Fundamental Truths
- UMC: Articles of Religion 및 Foundational Documents

시드 데이터에는 원문 전체를 넣지 말고 교단 정보, 자료원 URL, 문서 유형, 예상 공식성만 등록한다. 첫 수집 전 관리자가 `license_status`와 활성화를 확인해야 한다.

URL 검증 규칙:

- HTTPS 우선
- 리디렉션 후 최종 호스트도 허용목록 검증
- 사설 IP, localhost, 메타데이터 주소 차단
- 다운로드 크기·시간·리디렉션 횟수 제한
- 허용 MIME 유형만 처리
- robots.txt와 이용약관 기록
- 로그인·CAPTCHA·접근 제한 우회 금지

---

## 5. 수집 및 변경 감지 파이프라인

### 5.1 변경 감지

1. HEAD 또는 조건부 GET으로 ETag와 Last-Modified 확인
2. 서버가 이를 제공하지 않으면 정상화된 본문 또는 파일의 SHA-256 비교
3. 동일 해시면 새 문서와 새 임베딩을 생성하지 않음
4. 변경이면 원본 스냅샷을 먼저 MinIO에 저장
5. 기존 문서는 유지하고 새 `doctrine_documents` 판본을 생성
6. 최신판 여부는 관리자 승인 후 반영

### 5.2 객체 저장 경로

다음 의미를 갖는 결정적 키를 사용한다.

```text
doctrine-archive/{denomination_code}/{source_id}/{edition_or_date}/{content_hash}/original.{ext}
doctrine-archive/{denomination_code}/{source_id}/{edition_or_date}/{content_hash}/metadata.json
```

원본 객체는 불변으로 취급하고, 파생 텍스트와 OCR 결과는 별도 접두사에 저장한다.

### 5.3 실패와 재시도

- 네트워크 일시 오류만 지수 백오프로 제한 재시도
- 401/403/404, MIME 불일치, 과대 파일, 해시 불일치는 자동 반복 금지
- 동일 작업의 중복 실행을 분산 잠금 또는 고유 작업 키로 방지
- 재처리는 기존 작업을 덮어쓰지 않고 관계를 기록

---

## 6. 본문 추출과 품질 검사

### HTML

- 내비게이션·광고·푸터·관련글 제외
- 제목, 장, 절, 조항 번호와 문단 순서 보존
- 원문 링크와 최종 URL 보존
- 스크립트 실행이 필요한 사이트는 별도 승인 없이는 헤드리스 브라우저 수집을 추가하지 않음

### PDF

- 텍스트 레이어가 있으면 직접 추출
- 스캔 문서만 OCR 실행
- 페이지 번호와 섹션 위치 보존
- OCR 신뢰도가 기준 미만이면 `NEEDS_REVIEW`
- 빈 페이지, 반복 머리말, 글자 깨짐, 비정상 문자 비율 검사

### 품질 게이트

- 최소 본문 길이
- 추출 페이지 수와 원본 페이지 수 비교
- 문자 깨짐 비율
- 제목·조항 구조 인식률
- 원본과 추출 텍스트 표본 대조
- 언어 감지와 설정 언어 일치

품질 검사 실패 문서는 임베딩하지 않는다.

---

## 7. 구조 기반 청킹과 메타데이터

분할 우선순위는 `문서 → 편 → 장 → 절 → 조항/문답 → 문단 → 토큰 길이`로 한다. 한 조항이나 한 문답은 가능한 한 중간에서 자르지 않는다.

- 목표 크기: 약 400~700 토큰
- 중첩: 약 60~100 토큰
- 긴 조항만 문단 경계에서 추가 분할
- 각 청크에 완전한 `section_path` 부여
- 성경 참조는 원문 문자열과 정규화 값을 함께 보존
- 교단, 공식성, 채택 연도는 LLM 추출값이 아니라 승인된 자료원·문서 레코드에서 상속
- LLM 생성 태그는 `generated_metadata`로 분리하고 검증 전 권위 메타데이터로 사용하지 않음

청킹 결과는 결정적이어야 하며 동일 원문·동일 설정으로 재실행할 때 같은 청크 해시가 나와야 한다.

---

## 8. 임베딩과 pgvector 색인

1. 기존 프로그램의 임베딩 Provider와 차원을 우선 재사용한다.
2. 모델명, 모델 버전, 차원, 거리 척도를 문서 또는 색인 버전에 기록한다.
3. 모델 변경 시 기존 벡터를 덮어쓰지 말고 재색인 작업으로 관리한다.
4. HNSW 또는 기존 인덱스를 사용하되 메타데이터 필터 정확성을 별도 검증한다.
5. 교단·언어·공식성처럼 자주 쓰는 필터 열에 B-tree 인덱스를 추가한다.
6. 선택도가 낮거나 결과 부족이 발생하면 pgvector 반복 스캔을 검토한다.

예시 설정은 설치 버전에서 지원될 때만 적용한다.

```sql
SET LOCAL hnsw.iterative_scan = strict_order;
```

근사 검색 결과를 필터 적용 정확 검색과 비교하는 통합 테스트를 작성한다. 결과 부족, 순위 차이, 지연시간을 기록하고 운영 기본값을 결정한다.

---

## 9. 검색 정책과 교단 격리

기본 검색에는 반드시 다음 조건을 서버에서 강제한다.

```text
review_status = APPROVED
document active = true
source active = true
official_status in 사용자가 허용한 범위
denomination_code = 선택 교단
language = 선택 언어 또는 명시된 대체 언어
```

검색 모드:

| 모드 | 정책 |
|---|---|
| 선택 교단 우선 | 선택 교단의 승인된 공식 문서만 |
| 공통 교리 | 승인된 공교회 신조 컬렉션 우선 |
| 교단 비교 | 교단별 검색을 각각 실행하고 결과를 분리 |
| 역사적 비교 | 판본·채택 연도별로 결과 분리 |
| 전체 참고 | 공식·연구·참고 자료의 등급을 명시 |

교단 비교 모드에서도 서로 다른 교단의 문장을 하나의 인용문으로 합치지 않는다. 검색 결과가 없으면 다른 교단 문서로 몰래 대체하지 말고 부족 사실을 표시한다.

기존 하이브리드 검색이 있다면 어휘 검색과 벡터 검색을 함께 사용하되, 최종 후보마다 동일한 권한·상태·교단 필터를 다시 검증한다.

---

## 10. API 및 관리자 화면

기존 API 스타일을 따르며 다음 기능을 제공한다.

- 교단 목록·등록·수정·비활성화
- 자료원 목록·등록·검증·수정·비활성화
- 즉시 변경 확인 및 예약 주기 설정
- 수집 작업·오류·재처리 조회
- 원본과 추출 본문 비교 미리보기
- 문서 메타데이터 수정 및 승인·거절
- 청크 미리보기와 색인 실행
- 판본 목록·최신판 지정·이전판 복원
- RAG 검색 시험과 필터·출처 확인

관리자 화면은 다음 네 영역으로 단순화한다.

1. 교단 관리
2. 공식 자료원 관리
3. 변경 문서 검토
4. RAG 등록 현황

위험 작업에는 확인 절차와 감사 로그를 적용한다. 일반 사용자는 자료원 추가, 승인, 재색인, 원본 다운로드 권한을 갖지 않는다.

---

## 11. 설교 생성 결과의 출처 표시

각 근거에는 다음을 표시한다.

- 문서명
- 교단명과 교리 전통
- 공식성 상태
- 채택 연도와 판본
- 조항·문답·섹션·페이지
- 공식 출처 URL
- 수집 또는 확인 일자

답변은 가능하면 다음 세 층을 구분한다.

1. 성경 본문이 직접 말하는 내용
2. 선택 교단의 신앙고백·교리 문서가 설명하는 내용
3. AI가 구성한 해설과 설교 적용

인용 근거가 검색 결과에 없으면 해당 교단의 주장으로 단정하지 않는다. 원문 인용은 필요한 범위로 제한하고 출처로 이동할 수 있게 한다.

---

## 12. 저작권·보안·감사 요구사항

- 자료원마다 `license_status`: `VERIFIED`, `PUBLIC_DOMAIN`, `PERMISSION_REQUIRED`, `UNKNOWN`, `BLOCKED`를 관리
- `UNKNOWN`과 `PERMISSION_REQUIRED`는 기본 자동 색인 금지
- 유료·회원 전용 문서의 인증 우회 금지
- SSRF, 압축폭탄, 악성 PDF, 과대 파일, MIME 위장 방어
- 원본 파일 검사 실패 시 격리하고 파싱 금지
- 관리자 행위, 상태 변경, 최신판 지정, 삭제·비활성화를 감사 로그에 기록
- 로그에 원문 전체, 비밀키, 인증 헤더를 기록하지 않음
- 문서 삭제 요청과 검색 제외를 지원하되 감사·법적 보존 정책은 별도로 적용

---

## 13. 테스트 계획

### 단위 테스트

- URL 허용목록과 SSRF 차단
- ETag·Last-Modified·해시 변경 감지
- 동일 해시 중복 방지
- 상태 머신 허용·거부 전이
- HTML/PDF 구조 추출
- OCR 품질 임계값
- 구조 기반 청킹과 결정적 해시
- 교단·공식성·언어 필터
- 출처 표시 조립

### 통합 테스트

- 자료원 등록 → 변경 감지 → MinIO 보존 → 파싱 → 검토 → 승인 → 임베딩 → 검색 전체 흐름
- 승인 전 문서가 검색되지 않는지 확인
- 최신판 추가 시 구판이 보존되는지 확인
- 선택 교단 검색에 다른 교단 청크가 0건인지 확인
- 교단 비교 결과가 교단별로 분리되는지 확인
- 임베딩 실패 후 안전한 재처리
- MinIO 또는 DB 장애 시 부분 완료 데이터 정합성
- HNSW 필터 검색과 정확 검색 결과 대조

### 회귀·성능 테스트

- 기존 일반 RAG 검색 결과와 업로드 기능이 유지되는지 확인
- 대표 문서 10개 및 예상 운영 규모에서 수집·색인·검색 시간과 메모리 측정
- 중복 스케줄 실행 시 작업이 한 번만 수행되는지 확인
- 브라우저에서 관리자 승인과 사용자 교단 선택 흐름 확인

테스트에서 외부 공식 사이트를 반복 호출하지 말고 저장된 합법적 테스트 픽스처와 모의 HTTP 응답을 우선 사용한다.

---

## 14. 단계별 구현 순서와 완료 조건

### Phase 0 — 읽기 전용 조사

- 구조·의존성·위험·기준선 보고
- 소스 수정 금지

완료 조건: 실제 저장소 기준 구현 계획과 변경 대상 목록이 승인 가능할 정도로 명확함.

### Phase 1 — 데이터 모델과 마이그레이션

- 테이블, 열거형, 인덱스, 제약조건, 롤백 구현

완료 조건: 빈 DB와 기존 DB 모두 마이그레이션/롤백 테스트 통과.

### Phase 2 — 자료원·변경 감지·원본 보존

- 허용목록, 안전한 다운로드, 해시, 스냅샷, MinIO 구현

완료 조건: 동일 원본 재실행은 중복 생성 0건, 변경 원본은 새 판본 생성.

### Phase 3 — 파싱·OCR·청킹

- 구조 보존 추출과 품질 게이트 구현

완료 조건: 샘플 HTML/PDF/스캔 PDF 테스트 및 원문 위치 추적 통과.

### Phase 4 — 검토·승인·색인

- 상태 머신, 관리자 승인, 임베딩, 재색인 구현

완료 조건: 승인 전 검색 0건, 승인 후에만 검색 가능.

### Phase 5 — 검색 격리와 출처 표시

- 검색 모드, 강제 필터, 비교 결과 분리, 인용 표시 구현

완료 조건: 다른 교단 혼입 0건, 모든 결과에 추적 가능한 출처 포함.

### Phase 6 — UI·스케줄러·운영 안정화

- 관리자 대시보드, 예약 확인, 오류·재처리·감사 로그 구현

완료 조건: 브라우저 흐름과 운영 장애 시나리오 검증 통과.

각 Phase 종료 시 테스트 결과와 변경 파일을 보고하고 다음 Phase로 넘어간다. 대규모 일괄 변경은 피한다.

---

## 15. 롤백 및 장애 처리

- 기능 플래그로 교단 RAG를 즉시 비활성화할 수 있게 한다.
- 비활성화해도 기존 일반 RAG와 설교 생성은 정상 동작해야 한다.
- 새 판본 문제 발생 시 이전 승인 판본을 다시 활성화한다.
- 재색인 중에는 현재 운영 인덱스를 유지하고 완료 후 전환한다.
- 마이그레이션 롤백은 기존 테이블·데이터를 손상시키지 않아야 한다.
- 부분 실패 시 MinIO 객체, DB 레코드, 벡터 상태를 대조하는 복구 명령 또는 관리자 동작을 제공한다.

---

## 16. 최종 검수 체크리스트

- [ ] 공식 출처 허용목록 밖의 자동 수집이 차단됨
- [ ] 저작권 상태 미확인 문서가 자동 색인되지 않음
- [ ] 원본과 모든 판본이 MinIO에 불변 보존됨
- [ ] 동일 해시가 중복 임베딩되지 않음
- [ ] 공식 채택·역사·연구·제안 문서가 구분됨
- [ ] 승인 전 문서가 검색되지 않음
- [ ] 선택 교단 외 문서가 기본 검색에 섞이지 않음
- [ ] 교단 비교 결과가 명확히 분리됨
- [ ] 모든 근거에 문서·판본·조항·URL이 표시됨
- [ ] HNSW 필터 검색의 결과 부족 가능성이 검증됨
- [ ] 기존 일반 RAG 회귀 테스트가 통과함
- [ ] 기능 플래그와 판본 롤백이 검증됨
- [ ] 관리자·일반 사용자 권한이 분리됨
- [ ] 비밀정보와 원문 전체가 로그에 노출되지 않음

---

## 17. 최종 보고 형식

작업 완료 후 다음 형식으로 보고하라.

1. 구현 결과 요약
2. 실제 변경 파일 목록과 역할
3. DB 마이그레이션 및 롤백 방법
4. 자료원 등록·승인·재색인 사용법
5. 실행한 테스트와 통과/실패 결과
6. 기존 기능 회귀 검증 결과
7. 보안·저작권 검사 결과
8. 남은 제한사항과 후속 권장 작업

성공하지 않은 부분을 완료된 것처럼 표시하지 말고, 재현 조건과 안전한 다음 조치를 명시한다.

---

## 18. 근거 자료

- [pgvector 공식 저장소 및 필터·반복 스캔 설명](https://github.com/pgvector/pgvector)
- [기독교대한감리회 교리와 장정 2025](https://kmc.or.kr/combination-resources/gyoriwa-jangjeong/gyoriwa-jangjeong-2025?mode=detail&pid=195597)
- [한국기독교장로회 신앙고백서](https://www.prok.org/File/Download?paramFileU=1291)
- [한국기독교장로회 헌법·규칙집 안내](https://www.prok.org/Link/Index/11)
- [PCA Westminster Confession](https://www.pcaac.org/bco/westminster-confession/)
- [OPC Confessions and Catechisms](https://opc.org/confessions.html)
- [SBC Baptist Faith and Message 2000](https://bfm.sbc.net/bfm2000/)
- [Assemblies of God Statement of Fundamental Truths](https://ag.org/beliefs/statement-of-fundamental-truths)
- [UMC Foundational Documents](https://www.umc.org/en/content/foundational-documents)

