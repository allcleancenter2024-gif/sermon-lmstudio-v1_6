# V40.6.1 연결 복구 개선 롤백

V40.6.1은 LM Studio 추론 연결이 순간적으로 끊겼을 때 같은 모델로 한 번만 재시도하고, 화면에서 단계별 연결 복구 점검을 제공하는 변경입니다. 성경 DB 구조는 변경하지 않습니다.

## 이전 버전으로 되돌리기

1. 실행 중인 설교 프로그램 창을 닫습니다.
2. 현재 V40.6.1 폴더의 이름을 `V40.6.1-보관`처럼 변경합니다.
3. `SermonLMStudio-V40.6.0-FIXED13-before-lmstudio-reconnect-fix.zip`을 **새 빈 폴더**에 압축 해제합니다.
4. 기존 폴더에 덮어쓰거나 서로 다른 버전의 파일을 섞지 않습니다.
5. 압축 해제한 폴더의 실행 배치 파일을 실행합니다.

사용자 DB를 별도로 되돌릴 필요는 없습니다. 데이터까지 이전 시점으로 복원하려면 프로그램의 `로컬 데이터 통합 백업·복원`에서 검증된 백업 ZIP을 사용하십시오.
