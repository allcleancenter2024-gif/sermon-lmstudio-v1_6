# V40.6 복구 안내

## 데이터만 이전 상태로 되돌리기

1. 화면 아래 `로컬 데이터 통합 백업 · 복원`으로 이동합니다.
2. V40.6 변경 전에 내려받은 통합 백업 ZIP을 선택합니다.
3. `검사 후 복원`을 누릅니다.
4. 프로그램은 복원 직전 현재 DB를 자동 백업한 뒤 해시와 SQLite 무결성을 확인하고 복원합니다.

Notebook 연구노트와 Google Drive 폴더 설정도 DB에 저장되므로 통합 백업으로 함께 복구됩니다. 이미 Google Drive에 동기화된 자료팩 파일은 Google Drive에서 직접 삭제해야 하며, 프로그램이 원격 파일을 자동 삭제하지 않습니다.

## 프로그램 코드 전체를 V40.5로 되돌리기

1. 실행 중인 V40.6 서버 창을 닫습니다.
2. V40.6 폴더 이름을 `V40.6-보관`처럼 바꿉니다.
3. `SermonLMStudio-V40.5.0-FIXED12-before-notebooklm.zip`을 **새 빈 폴더**에 압축 해제합니다.
4. 해당 폴더의 `RUN_V40.bat`을 실행합니다.
5. 필요하면 V40.5에서 만든 통합 백업을 화면에서 복원합니다.

V40.5 파일과 V40.6 파일을 같은 폴더에 섞지 마세요. 실행기는 버전이 섞이면 안전을 위해 서버 시작을 중단합니다.

## Google Drive 기능만 끄기

`Gemini Notebook 연구 연결`에서 `설정 해제`를 누르고 `Google Drive 동기화 폴더에도 복사` 선택을 끕니다. 로컬 자료팩 생성과 기존 LM Studio 설교 작성은 계속 사용할 수 있습니다.
