# Phase 1 작업 지시서 — 교단 RAG 데이터 모델·마이그레이션 기반 구축

> 선행 문서: DENOMINATION_DOCTRINE_RAG_IMPLEMENTATION_INSTRUCTIONS.md  
> 선행 조건: Phase 0 읽기 전용 구조 조사와 기준선 보고 완료  
> 작업 범위: 데이터 모델, 마이그레이션, 저장소 계층, 상태 규칙, 단위 테스트  
> 이번 단계 제외: 웹 수집, OCR, 임베딩 생성, 실제 문서 자동 등록, 관리자 UI

---

## 1. Codex 실행 지시

DENOMINATION_DOCTRINE_RAG_IMPLEMENTATION_INSTRUCTIONS.md와 Phase 0 기준선 보고서를 먼저 전체적으로 읽어라.

이번 작업에서는 Phase 1인 교단 RAG 데이터 모델과 마이그레이션 기반만 구현하라. 웹 수집기, OCR, 임베딩 작업, 스케줄러, 관리자 UI 및 실제 교단 문서 등록은 구현하지 마라.

기존 저장소의 ORM, 마이그레이션 도구, Repository/Service 패턴, 명명 규칙과 테스트 방식을 그대로 따른다. 확인되지 않은 프레임워크나 디렉터리를 임의로 만들지 않는다.

작업 전 변경 예정 파일과 마이그레이션 설계를 보고하고, 작업 후 전체 테스트와 롤백 검증 결과를 한국어로 제출하라.

---

## 2. 이번 단계의 목적

교단별 신앙고백·교리 문서를 안전하게 수집하고 검색하기 위한 데이터 기반을 만든다. 일반 RAG 자료와 교단 문서가 논리적으로 분리되고, 다음 단계의 수집·검토·색인이 기존 기능에 충돌 없이 연결될 수 있어야 한다.

완료 시 다음 조건을 만족해야 한다.

- 교단, 공식 자료원, 문서 판본, 청크, 원본 스냅샷, 수집 작업을 표현할 수 있다.
- 공식 채택판·역사판·연구판·제안판·참고판을 구분할 수 있다.
- 문서와 청크의 중복을 데이터베이스 수준에서 방지한다.
- 승인되지 않은 문서를 검색하지 못하게 할 상태 기반을 마련한다.
- 최신판이 생겨도 과거 판본을 덮어쓰지 않는다.
- 기존 일반 RAG 테이블과 API의 동작을 유지한다.
- 마이그레이션 업그레이드와 안전한 다운그레이드를 검증한다.

---

## 3. 시작 전 필수 확인

소스를 수정하기 전에 다음을 실제 코드와 Phase 0 보고서에서 확인하라.

1. 현재 ORM과 마이그레이션 도구 및 버전
2. PostgreSQL과 pgvector 실제 버전
3. 기존 문서·청크·벡터 테이블 구조
4. 임베딩 열의 자료형, 차원, 거리 연산자
5. UUID 또는 정수 PK 사용 규칙
6. 타임스탬프와 시간대 규칙
7. JSON·배열·열거형 사용 관례
8. 소프트 삭제 또는 활성 상태 처리 방식
9. Repository·Service·DTO·Schema 계층
10. 테스트 DB와 마이그레이션 테스트 방법
11. 현재 작업 트리의 사용자 변경사항
12. MinIO 버킷 버전 관리·객체 잠금의 현재 설정

Phase 0 보고서와 실제 코드가 다르면 실제 코드를 기준으로 하되 차이를 보고한다. 선행 마이그레이션이 깨져 있거나 pgvector 차원이 확인되지 않으면 추측하여 진행하지 말고 중단 사유를 보고한다.

---

## 4. 변경 범위

### 구현 대상

- 교단 도메인 모델
- 교단 공식 자료원 모델
- 교리 문서와 판본 모델
- 교리 문서 청크 모델
- 원본 스냅샷 모델
- 수집·처리 작업 모델
- 필요한 열거형 또는 검증 상수
- PK, FK, UNIQUE, CHECK 제약
- 메타데이터 검색용 B-tree 인덱스
- pgvector 열과 벡터 인덱스 설계
- Repository 또는 기존 데이터 접근 계층
- 상태 전이 검증 로직
- 마이그레이션 up/down
- 모델·제약·Repository 단위 테스트

### 구현 금지

- 외부 URL 요청과 교단 홈페이지 크롤링
- 실제 PDF 다운로드
- MinIO 버킷 생성·삭제·잠금 설정 변경
- OCR과 문서 파서
- 임베딩 API 호출
- HNSW 운영 인덱스의 무조건 생성
- 관리자 UI
- 운영 시드 데이터의 자동 활성화
- 기존 일반 RAG 데이터 이전 또는 삭제

---

## 5. 필수 데이터 모델

실제 프로젝트 명명 규칙에 맞게 조정하되 아래 의미와 관계는 보존한다.

### 5.1 denominations

필수 필드 의미:

- 내부 식별자
- 고유한 교단 코드
- 한글명과 영문명
- 교리 전통
- 국가 코드
- 공식 홈페이지
- 활성 여부
- 생성·수정 시각

검증 규칙:

- 교단 코드는 대소문자 규칙을 하나로 정하고 고유하게 유지
- 공백만 있는 이름 금지
- 국가 코드는 기존 표준이 있으면 재사용
- 삭제보다 비활성화를 기본으로 사용

### 5.2 doctrine_sources

필수 필드 의미:

- 소속 교단 FK
- 자료원명과 문서 유형
- 공식 URL
- 출처 권위 등급
- 수집 방식과 갱신 주기
- 이용권한 상태
- 사람 검토 필요 여부
- 활성 여부와 마지막 확인 시각

출처 권위 등급:

- OFFICIAL_GENERAL_ASSEMBLY
- OFFICIAL_DENOMINATION
- OFFICIAL_AGENCY
- ACADEMIC_REFERENCE
- OTHER_REFERENCE

이용권한 상태:

- VERIFIED
- PUBLIC_DOMAIN
- PERMISSION_REQUIRED
- UNKNOWN
- BLOCKED

이번 단계에서는 URL을 호출하지 않는다. URL 형식과 호스트 값만 저장하며 안전한 네트워크 검증은 Phase 2로 남긴다.

### 5.3 doctrine_documents

필수 필드 의미:

- 자료원 FK
- 문서 제목, 채택 연도, 판본, 언어
- 공식성 상태
- 발행·수집 시각
- SHA-256 원문 해시
- 객체 저장 키와 MIME 형식
- 검토 상태
- 대체한 이전 문서에 대한 자기참조 FK
- 확장 메타데이터와 활성 여부

공식성 상태:

- OFFICIALLY_ADOPTED
- HISTORICAL_STANDARD
- STUDY_EDITION
- PROPOSED
- REFERENCE_ONLY
- UNKNOWN

검토 상태:

- DISCOVERED
- DOWNLOADED
- PARSED
- NEEDS_REVIEW
- APPROVED
- INDEXED
- REJECTED
- FAILED
- SUPERSEDED
- DISABLED

원본 해시는 고정 길이 또는 형식 검증을 적용한다. 동일 자료원에서 같은 원본 해시가 중복 판본으로 만들어지지 않게 한다. supersedes_document_id는 자기 자신을 참조할 수 없다.

### 5.4 doctrine_chunks

필수 필드 의미:

- 문서 FK
- 섹션 전체 경로
- 조항·문답 번호와 제목
- 문서 내 청크 순번
- 본문과 임베딩
- 토큰 수
- 성경 참조와 주제 태그
- 청크 해시
- 임베딩 모델·버전 또는 임베딩 버전 참조
- 생성·수정 시각

제약:

- 문서별 청크 순번 고유
- 문서별 청크 해시 중복 금지
- 빈 본문 금지
- 청크 순번과 토큰 수는 음수 금지
- 임베딩은 다음 단계까지 NULL 허용

임베딩 차원은 기존 프로그램에서 확인한 값을 재사용한다. 차원이 불명확하면 임의의 vector(1536), vector(768) 등을 만들지 않는다.

### 5.5 source_snapshots

필수 필드 의미:

- 자료원 FK
- 확인 시각
- HTTP 상태
- ETag와 Last-Modified
- 콘텐츠 해시
- 객체 저장 키
- 변경 감지 여부
- 안전한 오류 요약

이번 단계에서는 저장 구조만 만든다. 실제 HTTP 확인은 Phase 2에서 구현한다.

### 5.6 ingestion_jobs

필수 필드 의미:

- 자료원·문서 선택 FK
- 작업 유형과 작업 상태
- 멱등성 키
- 시도 횟수
- 오류 코드와 안전한 오류 요약
- 시작·종료 시각
- 재처리 원본 작업 FK
- 확장 메타데이터

동일 멱등성 키의 활성 작업이 중복 생성되지 않게 한다. 비밀키, 인증 헤더, 원문 전체를 오류 필드에 저장하지 않는다.

---

## 6. 관계·삭제 정책

- 교단 삭제가 자료원과 문서를 연쇄 삭제하지 않게 한다.
- 운영 데이터는 물리 삭제보다 active=false 또는 상태 변경을 사용한다.
- 문서가 참조된 상태에서 원본 자료원을 삭제하지 못하게 한다.
- 문서 삭제가 청크·감사 이력을 무의식적으로 제거하지 않게 한다.
- supersedes_document_id의 이전 판본이 사라지지 않게 한다.
- 테스트 픽스처 정리는 별도 경로로 처리한다.

기존 프로젝트의 삭제 정책이 다르면 그 이유와 위험을 보고하고 일관된 대안을 적용한다.

---

## 7. 제약조건과 인덱스

최소 다음 조회 경로를 고려한다.

- 교단 코드
- 자료원별 활성 문서
- 교단·언어·공식성·검토 상태
- 자료원·원본 해시
- 문서·청크 순번
- 문서·청크 해시
- 변경 감지 시각
- 작업 상태와 다음 재시도 대상

주의사항:

1. UNIQUE 제약이 생성하는 인덱스를 중복 생성하지 않는다.
2. PostgreSQL은 참조하는 FK 열의 인덱스를 자동으로 항상 만들지 않으므로 실제 조회·삭제 경로에 맞춰 추가한다.
3. JSON 전체에 무조건 GIN 인덱스를 만들지 않는다.
4. HNSW는 데이터 규모와 기존 전략을 확인한 후 결정한다.
5. 운영 환경에서 장시간 잠금을 유발할 인덱스 생성은 이번 단계에서 피한다.
6. pgvector 0.8 이상에서 필터 후 결과 부족을 줄이는 반복 스캔을 사용할 수 있으나, 이번 단계에서는 버전 확인과 테스트 계획까지만 확정한다.
7. partial index 또는 파티셔닝은 실제 규모 근거 없이 조기 도입하지 않는다.

---

## 8. 상태 전이 규칙

기본 흐름:

DISCOVERED → DOWNLOADED → PARSED → NEEDS_REVIEW → APPROVED → INDEXED

예외 흐름:

- 처리 실패 시 FAILED
- 검토 거절 시 REJECTED
- 새 판본 채택 시 이전 판본 SUPERSEDED
- 운영 제외 시 DISABLED
- 실패 작업의 재처리는 새 작업으로 생성

필수 검증:

- 임의 상태 점프를 Service 계층에서 차단
- APPROVED 이전에는 INDEXED 전이 금지
- BLOCKED 또는 UNKNOWN 이용권한 자료원의 문서는 승인 금지
- SUPERSEDED 문서의 자동 재활성화 금지
- 상태 변경 감사 확장 지점 마련

DB CHECK 제약과 애플리케이션 검증의 역할을 구분한다. 복잡한 전이를 DB ENUM만으로 해결하지 않는다.

---

## 9. Repository·Service 구현

기존 패턴이 있을 때만 해당 계층을 추가한다.

필수 동작:

- 교단 코드로 조회
- 자료원 생성·수정·비활성화
- 자료원별 동일 해시 문서 조회
- 문서 판본 생성
- 이전 판본과 새 판본 연결
- 문서 상태 전이
- 승인된 활성 문서 조회
- 문서별 청크 일괄 등록을 위한 인터페이스
- 멱등성 키로 작업 조회

이번 단계에서는 외부 네트워크, MinIO 저장, 임베딩 Provider를 호출하지 않는다. 다음 Phase가 필요한 최소 경계만 만들고 빈 추상화 계층을 과도하게 만들지 않는다.

---

## 10. 마이그레이션 작성 규칙

1. 기존 마이그레이션 파일을 수정하지 말고 새 마이그레이션을 추가한다.
2. 현재 DB와 빈 DB에서 모두 적용 가능해야 한다.
3. 테이블 → FK → 인덱스 순서와 다운그레이드 역순을 검증한다.
4. 운영 테이블 전체 재작성이나 장시간 잠금 가능성을 보고한다.
5. 기존 pgvector 확장을 중복 생성하지 않는다.
6. 확장이 없을 때는 프로젝트 표준 처리 방식을 따른다.
7. 실제 교단 원문이나 운영 시드 데이터를 넣지 않는다.
8. 다운그레이드는 이번 단계에서 만든 객체만 제거한다.
9. 사용자 데이터가 존재하는 환경의 다운그레이드 위험을 명시한다.
10. PostgreSQL·ORM 버전에서 지원되지 않는 문법을 쓰지 않는다.

---

## 11. MinIO 관련 제한

DB에는 object_storage_key와 필요한 메타데이터만 준비한다.

- 기존 버킷을 생성·삭제·변경하지 않는다.
- 객체 잠금이나 WORM을 자동 활성화하지 않는다.
- 버킷 버전 관리 상태는 읽기 전용으로만 확인한다.
- 객체 잠금이 기존 버킷과 호환되지 않으면 별도 신규 버킷 제안으로 남긴다.
- 실제 원본 저장과 불변성 구현은 Phase 2에서 수행한다.

---

## 12. 필수 테스트

### 모델·제약 테스트

- 중복 교단 코드 거부
- 같은 자료원·같은 원본 해시 중복 거부
- 문서별 청크 순번과 해시 중복 거부
- 빈 청크·음수 토큰·음수 순번 거부
- 자기 자신을 대체 판본으로 지정하는 동작 거부
- 잘못된 FK 거부
- 허용되지 않은 상태 전이 거부
- 승인 전 INDEXED 전이 거부
- 권한 상태 미확인 자료원 문서 승인 거부

### Repository·Service 테스트

- 교단 생성·조회·비활성화
- 자료원 등록
- 새 문서 판본 등록
- 동일 해시 멱등 처리
- 이전·새 판본 연결
- 승인된 활성 문서만 조회
- 다른 교단 문서가 필터 결과에 섞이지 않음
- 활성 작업 멱등성 키 중복 방지

### 마이그레이션 테스트

1. 빈 테스트 DB에 최신 버전까지 upgrade
2. 새 마이그레이션 downgrade
3. 다시 upgrade
4. 테이블·제약·인덱스 존재 확인
5. 기존 일반 RAG 테스트 실행
6. 기존 데이터가 변하지 않았는지 확인

운영 DB에서는 마이그레이션 테스트를 수행하지 않는다.

---

## 13. 완료 조건

- [ ] Phase 0 결과를 반영한 실제 프로젝트 방식으로 구현됨
- [ ] 기존 일반 RAG와 교단 RAG가 논리적으로 분리됨
- [ ] 모든 필수 모델과 관계가 구현됨
- [ ] 고유성·FK·CHECK 제약이 적용됨
- [ ] 필요한 필터 열 인덱스가 있고 중복 인덱스가 없음
- [ ] 임베딩 차원을 추측하지 않음
- [ ] 상태 전이 규칙이 Service와 테스트에 반영됨
- [ ] 승인 전 문서를 조회 대상에서 제외할 기반이 있음
- [ ] 과거 판본을 덮어쓰지 않는 구조임
- [ ] upgrade → downgrade → upgrade 검증 통과
- [ ] 기존 전체 테스트와 RAG 회귀 테스트 통과
- [ ] 외부 URL·MinIO·임베딩 API를 호출하지 않음
- [ ] 실제 교단 원문과 운영 시드 데이터를 넣지 않음

한 항목이라도 실패하면 Phase 1을 완료로 표시하지 않는다.

---

## 14. 중단 조건

다음 중 하나가 발생하면 억지로 우회하지 말고 중단하여 보고한다.

- Phase 0 보고서가 없거나 실제 코드와 중대한 불일치
- 기존 마이그레이션 체인이 깨져 있음
- pgvector 확장 또는 임베딩 차원을 확인할 수 없음
- 사용자 작업과 동일 파일에서 충돌
- 운영 DB만 존재하고 안전한 테스트 DB가 없음
- 모델 분리가 기존 핵심 API를 깨뜨릴 가능성이 큼
- 다운그레이드가 사용자 데이터를 훼손할 위험
- 필요한 설계 선택이 사용자의 운영 정책을 변경함

---

## 15. 최종 보고 형식

작업 완료 후 한국어로 다음을 보고한다.

1. 구현 결과 요약
2. Phase 0 결과에서 반영한 사항
3. 변경·추가 파일과 각 역할
4. 생성한 테이블·관계·제약·인덱스
5. 마이그레이션 upgrade/downgrade 명령
6. 실행한 테스트와 실제 결과
7. 기존 RAG 회귀 검증 결과
8. 발견한 위험과 미해결 항목
9. 롤백 방법
10. 다음 Phase 2에서 구현할 범위

마지막에는 다음 판정 중 하나만 제시한다.

- Phase 1 완료 — Phase 2 진행 가능
- 조건부 완료 — 보완 후 Phase 2 가능
- Phase 1 중단 — 원인 해결 필요

---

## 16. 공식 기술 근거

- [pgvector 공식 문서 — 필터링과 반복 인덱스 스캔](https://github.com/pgvector/pgvector)
- [PostgreSQL 공식 문서 — 제약조건과 고유 인덱스](https://www.postgresql.org/docs/current/ddl-constraints.html)
- [PostgreSQL 공식 문서 — 테이블 파티셔닝](https://www.postgresql.org/docs/current/ddl-partitioning.html)
- [MinIO 공식 문서 — 객체 버전 관리](https://docs.min.io/aistor/administration/objects-and-versioning/versioning/enable-versioning/)
- [MinIO 공식 문서 — 객체 잠금과 불변성](https://docs.min.io/aistor/administration/object-locking-and-immutability/)
- [OWASP — SSRF 방지 지침](https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html)

