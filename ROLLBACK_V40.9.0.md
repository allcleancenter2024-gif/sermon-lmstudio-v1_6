# V40.9.0 스트리밍 추론 롤백

V40.9.0은 LM Studio의 긴 설교 생성을 SSE 스트리밍으로 수신하도록 통신 구조를 변경합니다. 성경·설교 DB 구조는 변경하지 않습니다.

1. V40.9.0 콘솔을 닫습니다.
2. 현재 폴더 이름을 `V40.9.0-보관`으로 변경합니다.
3. `SermonLMStudio-V40.8.1-FIXED18-EasyUI-BackToTop.zip`을 새 빈 폴더에 풉니다.
4. LM Studio의 Local Server 포트는 동일하게 `12345`를 사용합니다.

`data/bible.db`를 이어서 사용하려면 프로그램이 완전히 종료된 상태에서만 백업본을 복사하세요.
