# Golden Samples

Each supported profile has an input and approved metadata. Expected output snapshots are reviewed artifacts, not automatically regenerated files. Record profile, version, locale, Unicode/table/source flags, reason, and review context when changing a baseline.
