# Rollback Archive

Use Git history plus a configuration snapshot as the rollback archive. Record the last known-good test result, Legacy file inventory, rollout values, symptoms, decision owner, configuration rollback, code rollback, and post-rollback checks. Emergency rollback changes the rollout setting to `legacy` and keeps V2 disabled.
