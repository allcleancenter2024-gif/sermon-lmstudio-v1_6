# Accessibility

Check heading hierarchy, table headers, keyboard focus, contrast, meaningful links, non-color status, 200% zoom, mobile viewport, and Unicode readability. Use semantic elements before ARIA; add ARIA only where native semantics are insufficient.
