# Security

Escape all untrusted text. Reject script elements, inline event handlers, `javascript:` URLs, trackers, and unapproved iframes. Rendering must not invoke shell commands or interpret document text as instructions.
