# Print Rules

Use A4-friendly margins, print colors, page-break rules, wrapped code, source visibility, table splitting, and footer handling. Prefer the existing PDF engine through an adapter. Do not reconstruct content separately for PDF.
