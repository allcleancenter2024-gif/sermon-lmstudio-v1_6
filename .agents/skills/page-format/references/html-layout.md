# HTML Layout

Use standalone UTF-8 semantic HTML with one H1, responsive CSS variables, native tables, readable source cards, print baseline, and no remote assets. Keep long code and tables scrollable or wrapped on narrow screens.
