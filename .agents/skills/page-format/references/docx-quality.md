# DOCX Quality

Check the ZIP package, `word/document.xml`, title/heading/body structure, sources, tables, and Unicode. Reuse the existing python-docx exporter and do not create redundant business-specific styles.
